#!/bin/bash

#PBS -N hello
#PBS -L tasks=1:lprocs=1:swap=512mb
#PBS -l walltime=2:00

# Make sure we are in the right directory in case writing files 
cd $PBS_O_WORKDIR

# Build the environment
module load calcua/2019b
module load intel/2019b

# Run the program
./hello > output-torque.$PBS_JOBID
