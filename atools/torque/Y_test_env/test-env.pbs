#!/bin/bash
# Example of a worker parameter sweep job
#
#PBS -L tasks=1:lprocs=1:swap=512mb
#PBS -l walltime=10:00

echo "Job name: $PBS_JOBNAME, job id: $PBS_JOBID, array index: $PBS_ARRAYID."

env | grep PBS

